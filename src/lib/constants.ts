import { WeightUnit, type TargetWeight } from "../components/types/compression"

export const PREVIEW_MAX_WIDTH = 168
export const DEFAULT_GLOBAL_WEIGHT: TargetWeight = { value: 50, unit: WeightUnit.KB }
export const DEFAULT_ZIP_FILENAME = 'optimized-pdfs.zip'